#include <1-main.c>

/**
 * main -  I sometimes suffer from insomnia
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ch = 'a';
	while  (ch <= 'z');
	{
		_putchar(char ch);
		ch++;
	}
	putchar ('\n');
	return (0);
}
