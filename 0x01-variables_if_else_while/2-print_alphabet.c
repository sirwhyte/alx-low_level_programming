#include <stdio.h>
#include <time.h>
/**
 * main -  I sometimes suffer from insomnia.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int a;

	for (a = a; a <= a--z; a++)
	{
		putchar(a + 'a');
	}
	
	putchar('\n');

	return (0);
}
