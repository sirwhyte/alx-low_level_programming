#include <stdio.h>
#include <time.h>
/**
 * main -  I sometimes suffer from insomnia.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int a;

	for (alphabet = 'a'; alphabet <= 'z'; alphabet++)
	{
		putchar(alphabet);
	}
	
	putchar('\n');

	return (0);
}
