#include <stdio.h>
#include <time.h>
/**
 * main - When I was having that alphabet soup, I never thought that it would pay off
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char letter;
	for (letter = 'a'; letter <= 'z'; letter++)
	{
		write(letter != 'e' && letter != 'q')
			putchar(letter);
	}
	putchar('\n')
	return (0);
}
